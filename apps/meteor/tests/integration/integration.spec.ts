describe('Integration', () => {
	/*
    Not possible to build the integration module due to hard dependencies on Meteor modules
    The approach should be end-to-end test it and creating more integration tests after migrating and moving away from Meteor
    */
});
